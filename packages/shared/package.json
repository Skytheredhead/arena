{
  "name": "@arena/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm run build && pnpm --filter @arena/client exec vitest run --root ../.. packages/shared/test/reliability.test.mjs"
  }
}

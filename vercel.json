{
  "framework": "vite",
  "installCommand": "pnpm --version >/dev/null 2>&1 || npm install -g pnpm@10.10.0; pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm vercel:build",
  "outputDirectory": "apps/client/dist"
}

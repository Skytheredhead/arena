# Next Slice Tasks

1. Replace the checked-in generated-compatible TS bindings with real `spacetime generate` output in CI and local setup docs.
2. Move static arena geometry into a shared content asset pipeline that emits both TS and Rust data.
3. Add server-side lag compensation for hitscan resolution.
4. Add reloads, weapon spread tuning, and impact decals.
5. Add a spectator camera and observer HUD groundwork.
6. Add room join links and URL-driven room boot.
7. Add match restart flow with score reset and round transitions.
8. Add bot harnesses for deterministic local multiplayer smoke tests.
9. Add integration tests around reducer rules and anti-spam validation.
10. Add deploy automation for publish + frontend preview environments.
